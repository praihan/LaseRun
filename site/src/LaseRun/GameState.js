this.LaseRun = this.LaseRun || {};

(function(undefined) {
    "use strict";

    var GameState = function() {
    }

    var p = GameState.prototype;

    p.preload = p.create = p.update = p.render = function() {
    }

    LaseRun.GameState = GameState;

})();